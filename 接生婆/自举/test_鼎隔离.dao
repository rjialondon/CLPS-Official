# test_鼎隔离.dao — 验证鼎卦命名空间隔离
# 如果隔离失败，子程序的变量会污染母层

# 设一个普通变量
乾 "消息" "母层原值" 火
兑 "消息"

# 设一个和解释器内部变量撞名的变量
乾 "g_cur_type" "我是子程序的g_cur_type" 火
兑 "g_cur_type"

# 如果上面没炸，说明子程序的g_cur_type没有覆盖解释器内部的g_cur_type
兑 "隔离成功"

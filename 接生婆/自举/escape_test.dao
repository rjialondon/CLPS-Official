# escape_test.dao — T10.0 转义序列测试
# 测试 lex_读字符串 对 \n \t \" \\ 的处理

# 测试1：含 \n 的字符串
乾 "a" "行一\n行二" 火
兑 "a"

# 测试2：含 \" 的字符串
乾 "b" "他说\"你好\"" 火
兑 "b"

# 测试3：含 \\ 的字符串
乾 "c" "路径\\文件" 火
兑 "c"

# 测试4：含 \t 的字符串
乾 "d" "列一\t列二" 火
兑 "d"

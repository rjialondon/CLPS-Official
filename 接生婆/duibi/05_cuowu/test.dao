# 道·码 蹇化：讼卦断言失败 → 蹇卦捕获 → 化卦处理
结界 "蹇化" {
    乾 "x" 5 金
    蹇 {
        讼 "x" > 10          # 断言失败（5 不大于 10），抛出异常
        乾 "msg" "断言通过" 火
        兑 "msg"             # 不会执行
    } 化 "err" {
        兑 "err"             # 输出错误信息
    }
}

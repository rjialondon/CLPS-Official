[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "taiji_core"
version = "0.1.0"
description = "Rust-accelerated TaijiVM core (TaiJiJieJie, GenXuLock, WuXing)"
requires-python = ">=3.7"

[tool.maturin]
features = ["pyo3/extension-module"]

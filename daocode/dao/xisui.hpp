#pragma once
/*
 * xisui.hpp — 洗髓丹·启动自检
 *
 * 洗髓 = 换掉最底层的骨髓。
 * 程序启动时探测运行环境能力，选择适配路径。
 *
 * 今日：所有硬件都是 0/1 二值底层，三位模拟八卦（乾=111...坤=000）。
 * 未来：原生八值硬件出现时，此函数自动切换路径，语言层零改动。
 *
 * 布丁已就位（面向未来编程）：
 *   接口写在今天，硬件追上来的那天，xi_sui_dan() 内部激活，
 *   外层调用者无感，CLPS 代码一行不动。
 */

#include <string>

// ── 运行模式 ──────────────────────────────────────────────────────────────
enum class XiSuiMode {
    BINARY_3BIT,   // 当前：0/1 底层，三位二进制模拟八卦
    OCTAL_NATIVE,  // 未来：原生八值硬件，直接映射（布丁，待激活）
};

// ── 自检结果 ──────────────────────────────────────────────────────────────
struct XiSuiResult {
    XiSuiMode   mode;
    std::string desc;   // 人类可读描述（中文）
    bool        native; // true = 已换醒为原生路径
};

// ── 洗髓丹：启动自检 ──────────────────────────────────────────────────────
// 探测运行环境能力，返回适配模式。
// 每次版本更新重新自检，条件成熟时自动切换。
XiSuiResult xi_sui_dan();
